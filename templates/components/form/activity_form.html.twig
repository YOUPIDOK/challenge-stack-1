{{ form_start(form) }}
<div class="row">
    <div class="col-md-6">{{ form_row(form.label) }}</div>
    <div class="col-md-3">{{ form_row(form.heartRate) }}</div>
</div>
<div class="row">
    <div class="col-md-3">{{ form_row(form.isDistance) }}</div>
</div>
<button class="btn btn-primary">{{ btn_label }}</button>
<a class="btn btn-outline-primary me-2" href="{{ path('activities') }}">Retour</a>
{{ form_end(form) }}