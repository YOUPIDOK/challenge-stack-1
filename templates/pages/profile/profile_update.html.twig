{% extends 'layouts/navbar_layout.html.twig' %}

{% block title %}Modifier mon profile{% endblock %}

{% block body %}
    <div class="container">
        <h1>Modifier mon profile</h1>

        {{ form_start(form) }}
        <div class="row">
            <div class="col-md-6">{{ form_row(form.email) }}</div>
        </div>
        <div class="row">
            <div class="col-md-6">{{ form_row(form.firstname) }}</div>
            <div class="col-md-6">{{ form_row(form.lastname) }}</div>
            <div class="col-md-6">{{ form_row(form.gender) }}</div>
            <div class="col-md-6">{{ form_row(form.client.birthdate) }}</div>
            <div class="col-md-6">{{ form_row(form.plainPassword.first) }}</div>
            <div class="col-md-6">{{ form_row(form.plainPassword.second) }}</div>
        </div>
        <div class="row mt-3">
            <div class="col-md-6">{{ form_row(form.client.height) }}</div>
            {% if weight is not null %}
                <div class="col-md-6">
                    {{ form_row(form.weight) }}
                    <a href="{# TODO LINK #}" class="link-primary">Modifier</a> mon poid
                </div>
            {% endif %}
        </div>

        <button class="btn btn-primary w-100 mt-3">Modifier</button>
        {{ form_end(form) }}
    </div>
{% endblock %}