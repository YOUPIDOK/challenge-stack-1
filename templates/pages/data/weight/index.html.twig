{% extends 'base.html.twig' %}

{% block title %}Weight index{% endblock %}

{% block body %}
    <h1>Weight index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Date</th>
                <th>Weight</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for weight in weights %}
            <tr>
                <td>{{ weight.id }}</td>
                <td>{{ weight.date ? weight.date|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ weight.weight }}</td>
                <td>
                    <a href="{{ path('app_main_data_weight_show', {'id': weight.id}) }}">show</a>
                    <a href="{{ path('app_main_data_weight_edit', {'id': weight.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_main_data_weight_new') }}">Create new</a>
{% endblock %}
