{% if chart.isValid %}
    <canvas
            data-controller="chart"
            data-chart-data-value='{{ chart.data }}'
            data-chart-type-value="{{ chart.type }}"
            data-chart-options-value="{{ chart.options }}"
    ></canvas>
{% endif %}